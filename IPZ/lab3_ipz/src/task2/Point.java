package task2;


/**
* Adaptee interface (existing): Cartesian point with integer coordinates.
* We are NOT allowed to change this interface.
*/
public interface Point {
    void setX(int x);
    void setY(int y);
    int getX();
    int getY();
}